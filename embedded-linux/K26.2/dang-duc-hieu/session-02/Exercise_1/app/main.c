#include <stdio.h>
#include "../include/mathutils.h"

int main() {
    printf("math_add: %d \n", math_add(2, 5));
    printf("math_sub: %d \n", math_sub(7, 2));
    printf("math_factorial: %d \n", math_factorial(5));
}