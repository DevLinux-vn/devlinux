Đầu tiên biến MY_NAME là biến regular ở trong bash parent do đó echo sẽ hiển thị tên đó lên ngay. sao đó STEP 2 tạo 1 bash khác ( kiểu như tạo 1 vùng nhớ riêng, 1 container độc lập-môi trường độc lập và chỉ chung các biến môi trường) thì biến MY_NAME của bash parent(khác vùng nhớ) do đó không in ra gì. STEP3 thì nâng cấp biến regular thành biến môi trường, do đó vào bash con(container khác) thì vẫn được chia sẻ biến môi trường và in ra. Lệnh exit để lại quay về bash parent, tiếp đó STEP4 tạo 1 container độc lập rồi mới khởi tạo biến regular(biến này như kiểu trong 1 hàm như code C, hết container này thì hết biến, nó như truyền tham trị trong cái container này, nên nó có thể ghi đè biến môi trường MY_NAME nhưng khi hết container này thì sẽ không có tác dụng do chỉ trong bash con này thôi) do đó vẫn in được echo ra là Alice, sau đó Exit ra bash parent thì vẫn giữ nguyên biến MY_NAME môi trường nên vẫn in ra Quan 